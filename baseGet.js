// 

import express from "express";
const app = express();

app.get("/", (req, res) => {
    const = req.query.
    const = req.query.
    const = 

    res.json({ message: `${} ${} ${}`});

});

app.listen(3000, () => {
    console.log("Servidor rodando na porta 3000");
});